# Dogfood notes — 2026-08-25

## Phase 0 reuse
- Minefield main `a4369c61533fb7577690048c0aa4fbf8e27f0b69` (#65) imported successfully.
- `plan_checks` / `run_checks` / `summarize` consumed in-process (no subprocess).

## Plugin
- Installed via symlink: `~/.hermes/plugins/hermes-minefield` → this repo.
- Enabled in `plugins.enabled`.
- `hermes minefield --help` lists check/doctor/wtf/incident/contribute/issues/status.

## Dogfood
- `hermes minefield status` — OK (fingerprint + recorder health).
- `hermes minefield doctor` without `--yes` — blocked on `total_slots=1` (PASS).
- Synthetic `/minefield wtf` prepare-storm — classified `HERMES_UI_ORCHESTRATION` (PASS).
- Contribute + GitHub draft dry-run — preview shown, no upload (PASS).
- Live `minefield check` against `:8007` — **NOT RUN** (slot `is_processing=true`; avoid contention).
- Recorder microbench: ~10.5 µs/event for 5000 events — overhead acceptable.

## Security
- 28 unit tests PASS including approval gates, repo allowlist, secret redaction, fixture A/B.
